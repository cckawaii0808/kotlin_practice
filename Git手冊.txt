
之後更動
git add .
git commit -m "增加不定義型態"
git push -u

git reset --hard HEAD 回復到最新提交版本
git reset --hard HEAD~ // 等於 ~1 回復到上一個提交版本
git reset --hard HEAD~n // n 等於往上第幾個提交版本 回復之前指定的提交版


git push -f  都聽我的

